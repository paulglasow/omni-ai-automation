import React from 'react';
import ReactDOM from 'react-dom/client';
import OmniAI from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <OmniAI />
  </React.StrictMode>
);
